apiVersion: v1
kind: Service
metadata:
  name: multimodal-rag-api
  labels:
    app: multimodal-rag-api
spec:
  type: ClusterIP
  selector:
    app: multimodal-rag-api
  ports:
    - port: {{ .Values.api.port }}
      targetPort: {{ .Values.api.port }}
      protocol: TCP
