apiVersion: v1
kind: Service
metadata:
  name: multimodal-rag-ui
  labels:
    app: multimodal-rag-ui
spec:
  type: LoadBalancer
  selector:
    app: multimodal-rag-ui
  ports:
    - port: 80
      targetPort: {{ .Values.ui.port }}
      protocol: TCP
